<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/highscoresLayout"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/goku_approves">

    <TextView
        android:text="@string/highscoresTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="40dp"
        android:textColor="@color/highscoresTitle"
        android:textAlignment="center"
        android:layout_marginTop="20dp"
        android:backgroundTint="@color/transparent"/>
    
    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/highscoresListView"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:background="#80439197"
        android:cacheColorHint="#00000000"
        android:divider="@color/transparent">
    </ListView>

    <TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/highscore_text_view2"
    android:paddingTop="2dip"
    android:paddingBottom="3dip"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@color/transparent"/>

</LinearLayout>